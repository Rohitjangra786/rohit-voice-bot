# Rohit voice bot — dependencies
# Pinned to a known-good range. Pipecat moves fast; if imports break,
# check https://docs.pipecat.ai for your installed version.

# Pipecat with the service extras Rohit needs:
#   deepgram  -> speech to text
#   anthropic -> the LLM (the "brain"; alternate to Groq)
#   groq      -> the LLM Groq path used by test_local.py and server_webrtc.py
#   cartesia  -> text to speech
#   silero    -> voice-activity detection (end-of-speech)
#   webrtc    -> aiortc + opencv, needed by server_webrtc.py
#   runner    -> FastAPI runner + prebuilt browser UI for server_webrtc.py
#
# For LOCAL mic+speakers testing (test_local.py) also run:
#   pip install 'pipecat-ai[local]'
# (skipped from this file because [local] pulls PyAudio, which needs
# portaudio system libs that aren't installed on Render/Railway by default.)
pipecat-ai[deepgram,anthropic,groq,cartesia,silero,webrtc,runner]>=1.2.0

# Web server + Twilio media-stream websocket
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0

# Telephony
twilio>=9.0.0

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
